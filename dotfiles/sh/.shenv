#!/bin/sh

if [ -x "`which uname`" ]; then
  export OS="`uname`"
fi
if [ -n "`echo ${OS} | grep CYGWIN`" ]; then
  OS='Cygwin'
fi

umask 022

# local setting
if [ -f ${HOME}/.shenv.local ]; then
  source ${HOME}/.shenv.local
fi
