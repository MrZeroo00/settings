# shell
# marker https://github.com/pindexis/marker # Marker is a command palette for the terminal.

# grep
#pip install --user timegrep

# powerline
pip install --user git+git://github.com/powerline/powerline
pip install --user powerline-status

# development
#pip install --user diff-highlight
#pip install --user blockdiag # blockdiag generates block-diagram images from .diag files (similar to graphviz’s DOT files).
#pip install --user frida # Dynamic instrumentation toolkit for developers, reverse-engineers, and security researchers.
#pip install --user mitmproxy # An interactive console program that allows traffic flows to be intercepted, inspected, modified and replayed.
#pip install --user Pygments # Python syntax highlighter
#pip install --user voltron # Voltron is an extensible debugger UI toolkit written in Python.
#pip install --user yamllint
#pip install --user yq
#pip install --user csvkit

# network
pip install --user pinggraph
# https://www.netresec.com/?download=trimpcap

# monitoring
#pip install --user glances

# AWS
pip install --user awslogs --ignore-installed six
pip install --user aws-shell
pip install --user saws

# other
#pip install --user thefuck # Magnificent app which corrects your previous console command.
#pip install --user twtxt # twtxt is a decentralised, minimalist microblogging service for hackers.

# jupyter
#pip install --user ipython
pip install --user jupyter
pip install --user jupyterlab
pip install --user pandas
pip install --user matplotlib
jupyter serverextension enable --py jupyterlab
jupyter lab
